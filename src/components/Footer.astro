---
const footerLinks = {
  product: [
    { label: 'Features', href: '/features/drift-detection' },
    { label: 'Integrations', href: '/integrations/react' },
    { label: 'Pricing', href: '/pricing' },
    { label: 'Use Cases', href: '/use-cases/design-system-teams' },
  ],
  resources: [
    { label: 'Documentation', href: '/docs' },
    { label: 'Blog', href: '/blog' },
    { label: 'Changelog', href: '/changelog' },
    { label: 'Community', href: '/community' },
  ],
  company: [
    { label: 'About', href: '/about' },
    { label: 'Contact', href: '/contact' },
    { label: 'Careers', href: '/careers' },
  ],
  connect: [
    { label: 'GitHub', href: 'https://github.com/buoy-dev/buoy', external: true },
    { label: 'Twitter', href: 'https://twitter.com/buoy_dev', external: true },
    { label: 'Discord', href: 'https://discord.gg/buoy', external: true },
  ],
}
---

<footer class="border-t border-navy-light/20 bg-navy-dark">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <div class="grid grid-cols-2 md:grid-cols-5 gap-8">
      <!-- Brand Column -->
      <div class="col-span-2 md:col-span-1">
        <a href="/" class="flex items-center gap-2 mb-4">
          <span class="text-2xl">🛟</span>
          <span class="text-xl font-bold font-[Satoshi] text-light">Buoy</span>
        </a>
        <p class="text-sm text-slate max-w-xs">
          Design drift detection for the AI era. Keep your codebase aligned with your design system.
        </p>
      </div>

      <!-- Product -->
      <div>
        <h4 class="font-semibold text-light mb-4">Product</h4>
        <ul class="space-y-2">
          {footerLinks.product.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-slate hover:text-buoy transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Resources -->
      <div>
        <h4 class="font-semibold text-light mb-4">Resources</h4>
        <ul class="space-y-2">
          {footerLinks.resources.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-slate hover:text-buoy transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Company -->
      <div>
        <h4 class="font-semibold text-light mb-4">Company</h4>
        <ul class="space-y-2">
          {footerLinks.company.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-slate hover:text-buoy transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Connect -->
      <div>
        <h4 class="font-semibold text-light mb-4">Connect</h4>
        <ul class="space-y-2">
          {footerLinks.connect.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-sm text-slate hover:text-buoy transition-colors"
                target="_blank"
                rel="noopener noreferrer"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="mt-12 pt-8 border-t border-navy-light/20 flex flex-col md:flex-row justify-between items-center gap-4">
      <p class="text-sm text-slate">
        &copy; {new Date().getFullYear()} Buoy. Open source under MIT License.
      </p>
      <div class="flex items-center gap-6 text-sm text-slate">
        <a href="/privacy" class="hover:text-light transition-colors">Privacy</a>
        <a href="/terms" class="hover:text-light transition-colors">Terms</a>
      </div>
    </div>
  </div>
</footer>
